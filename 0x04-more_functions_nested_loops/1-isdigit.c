#include "main.h"

/**
 *_isdigit - Output digit if condition is true
 *
 *@c: variable to be tested
 *
 *Return: 1 if condition is true
 */

int _isdigit(int c)
{
	if (c >= 'a' && c <= 'z')
		return (0);

	else
		return (1);
}
